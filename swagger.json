{
  "swagger": "2.0",
  "paths": {
    "/customer": {
      "get": {
        "description": "This will show all available customers by default.",
        "consumes": [
          "application/json"
        ],
        "produces": [
          "application/json"
        ],
        "schemes": [
          "http"
        ],
        "summary": "GetCustomers Get all customers",
        "operationId": "customers",
        "responses": {
          "200": {
            "description": "Customer",
            "schema": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Customer"
              }
            }
          }
        }
      }
    }
  },
  "definitions": {
    "Customer": {
      "description": "Type of customers",
      "type": "object",
      "required": [
        "id",
        "firstname",
        "lastname"
      ],
      "properties": {
        "firstname": {
          "description": "the name for this customer",
          "type": "string",
          "x-go-name": "Firstname"
        },
        "id": {
          "description": "the id for this customer",
          "type": "integer",
          "format": "int64",
          "minimum": 1,
          "x-go-name": "ID"
        },
        "lastname": {
          "description": "the lastname for this customer",
          "type": "string",
          "x-go-name": "Lastname"
        }
      },
      "x-go-package": "github.com/germangp088/customers/routes"
    }
  }
}